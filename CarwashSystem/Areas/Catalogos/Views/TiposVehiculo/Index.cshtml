@model IEnumerable<UI.Models.TiposVehiculo>
@{
    ViewData["Title"] = "Catálogo de Tipos de Vehículo";
}
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold text-info"><i class="bi bi-truck"></i> Tipos de Vehículo</h2>
        <button class="btn btn-info rounded-pill shadow" id="btnNuevo">
            <i class="bi bi-plus-circle"></i> Nuevo Tipo
        </button>
    </div>
    <div class="row">
        @foreach (var t in Model)
        {
            <div class="col-md-6 col-lg-4 mb-4">
                <div class="card shadow-sm border-0 rounded-4 h-100">
                    <div class="card-body">
                        <h5 class="card-title text-info">
                            <i class="bi bi-truck"></i> @t.Nombre
                        </h5>
                        <span class="badge rounded-pill @(t.Activo ? "bg-success" : "bg-secondary")">
                            <i class="bi @(t.Activo ? "bi-check-circle" : "bi-x-circle")"></i>
                            @(t.Activo ? "Activo" : "Inactivo")
                        </span>
                    </div>
                    <div class="card-footer bg-white border-0 d-flex gap-2 justify-content-end">
                        <button class="btn btn-outline-info btn-sm rounded-pill btnEditar" data-id="@t.Id">
                            <i class="bi bi-pencil-square"></i> Editar
                        </button>
                        <button class="btn btn-outline-danger btn-sm rounded-pill btnDesactivar" data-id="@t.Id" @(t.Activo ? "" : "disabled")>
                            <i class="bi bi-slash-circle"></i> Desactivar
                        </button>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

<!-- Modal para crear/editar -->
<div class="modal fade" id="modalTipo" tabindex="-1" aria-labelledby="modalTipoLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content rounded-4 shadow">
            <div class="modal-header bg-info text-white rounded-top-4">
                <h5 class="modal-title" id="modalTipoLabel"><i class="bi bi-truck"></i> Tipo de Vehículo</h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
            </div>
            <div class="modal-body" id="modalTipoBody">
                <!-- Aquí se carga el formulario por AJAX -->
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://code.jquery.com/jquery-3.7.1.min.js"></script>
    <script>
        // Nuevo
        $('#btnNuevo').click(function () {
            $.get('@Url.Action("Create")', function (html) {
                $('#modalTipoBody').html(html);
                $('#modalTipoLabel').text('Nuevo Tipo');
                var modal = new bootstrap.Modal(document.getElementById('modalTipo'));
                modal.show();
            });
        });

        // Editar
        $('.btnEditar').click(function () {
            var id = $(this).data('id');
            $.get('@Url.Action("Edit")/' + id, function (html) {
                $('#modalTipoBody').html(html);
                $('#modalTipoLabel').text('Editar Tipo');
                var modal = new bootstrap.Modal(document.getElementById('modalTipo'));
                modal.show();
            });
        });

        // Guardar (AJAX)
        $(document).on('submit', '#formTipo', function (e) {
            e.preventDefault();
            var form = $(this);
            var url = form.attr('action');
            $.post(url, form.serialize(), function (result) {
                if (result.success) {
                    location.reload();
                } else {
                    $('#modalTipoBody').html(result);
                }
            });
        });

        // Desactivar (AJAX)
        $('.btnDesactivar').click(function () {
            var id = $(this).data('id');
            if (confirm('¿Desactivar este tipo de vehículo?')) {
                $.post('@Url.Action("Desactivar")', { id: id }, function (result) {
                    if (result.success) location.reload();
                });
            }
        });
    </script>
}